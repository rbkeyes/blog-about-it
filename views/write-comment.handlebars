<div class="col-md-10 comment">
    <div class="row mt-4">
        <div class="col-md-12">
            
            {{!-- render blog content --}}
            {{#with blog}}
            {{> content}}
            {{/with}}

            {{!-- render comment form if indicated by write: true --}}
            {{#if write}}
            {{> comment-form}} 
            {{/if}}

            <a href="/api/comment/write/{{blog.id}}">Add a comment</a>

        </div>
    </div>
</div>